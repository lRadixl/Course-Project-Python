questions:
- answers:
    a: pd.DataFrame()
    b: pd.Series()
    c: pd.read_csv()
    d: pd.concat()
  correct_answer: a
  id: '1'
  question: Яка функція використовується для створення DataFrame у бібліотеці pandas?
- answers:
    a: df.shape
    b: df.info()
    c: df.describe()
    d: df.head()
  correct_answer: c
  id: '2'
  question: Який метод надає підсумкову статистику DataFrame у бібліотеці pandas?
- answers:
    a: df['column_name']
    b: df.loc[row_index]
    c: df.iloc[row_index]
    d: df.drop('column_name', axis=1)
  correct_answer: a
  id: '3'
  question: Як отримати доступ до певного стовпця в DataFrame у бібліотеці pandas?
- answers:
    a: df.dropna()
    b: df.fillna(value)
    c: df.isna()
    d: df.drop_duplicates()
  correct_answer: c
  id: '4'
  question: Який метод використовується для перевірки наявності пропущених значень в DataFrame у бібліотеці pandas?
- answers:
    a: df.groupby('column_name').mean()
    b: df.groupby('column_name').count()
    c: df.groupby('column_name').sum()
    d: df.groupby('column_name').max()
  correct_answer: b
  id: '5'
  question: Як обчислити кількість кожного унікального значення в стовпці за допомогою pandas?
- answers:
    a: df.sort_values('column_name', ascending=False)
    b: "df.rename(columns={'old_name': 'new_name'})"
    c: df.dropna(axis=0)
    d: df.drop_duplicates(subset='column_name')
  correct_answer: d
  id: '6'
  question: Як видалити дублікати рядків на основі певного стовпця в DataFrame у бібліотеці pandas?
- answers:
    a: df['new_column'] = df['column1'] + df['column2']
    b: df['new_column'] = df['column1'] * df['column2']
    c: df['new_column'] = df['column1'] / df['column2']
    d: df['new_column'] = df['column1'] - df['column2']
  correct_answer: a
  id: '7'
  question: Як створити новий стовпець у DataFrame, поєднуючи значення з двох існуючих стовпців у бібліотеці pandas?
- answers:
    a: df.groupby('column_name').mean()
    b: df.groupby('column_name').count()
    c: df.groupby('column_name').sum()
    d: df.groupby('column_name').max()
  correct_answer: c
  id: '8'
  question: Як обчислити суму кожної групи в стовпці за допомогою pandas?
- answers:
    a: df[df['column_name'] > value]
    b: df[df['column_name'] == value]
    c: df[df['column_name'] < value]
    d: df[df['column_name'] != value]
  correct_answer: b
  id: '9'
  question: Як відфільтрувати рядки в DataFrame на основі певної умови в стовпці?
- answers:
    a: df.sort_values('column_name', ascending=False)
    b: "df.rename(columns={'old_name': 'new_name'})"
    c: df.dropna(axis=0)
    d: df.drop_duplicates(subset='column_name')
  correct_answer: a
  id: '10'
  question: Як відсортувати DataFrame за значеннями в певному стовпці за допомогою pandas?
title: Тест Pandas